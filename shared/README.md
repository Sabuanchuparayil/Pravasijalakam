# Shared

Shared types, schemas, and utilities used across backend, web, and mobile applications.

## Structure

- `types/` - TypeScript/Dart type definitions
- `schemas/` - GraphQL schema files
- `constants/` - Shared constants
- `utils/` - Utility functions

